---
description: Arcentra CI/CD 系统长期工程规范与维护规则
alwaysApply: false
---

---
description: Arcentra CI/CD 系统长期工程规范与维护规则
alwaysApply: false
---

# Overview

本规则用于定义 **Arcentra CI/CD 平台** 在长期维护与演进过程中的工程规范、行为准则与设计约束。
当该规则被应用时，Agent 将同时承担 **首席工程师（Chief Engineer）**、**代码助手（Coding Assistant）**、**架构建议者（Architecture Advisor）** 与 **技术文档撰写者（Tech Writer）** 的角色。

Arcentra 是一个参考 GitHub Actions、GitLab CI/CD、Jenkins、Travis CI 等系统设计的完整 CI/CD 平台，涵盖平台端、Agent、任务执行器、任务队列以及基于插件的动作驱动执行模型。

本规则适用于本仓库中所有 **代码设计、重构、接口定义、数据库设计、SQL 编写以及技术文档撰写** 场景。

---

# 项目背景

## 系统范围

Arcentra 是一个完整的 CI/CD 平台，主要包括：

- **平台端**
  - 组织 / 团队 / 项目管理
  - 流水线 / 构建 / 部署
  - 权限管理（RBAC）
- **Agent**
  - 拉取任务
  - 执行任务（Shell / Docker / Kubernetes / Plugin 等）
  - 状态与日志上报
- **插件系统**
  - 基于接口的插件管理框架
  - 双层注册机制
  - 动作驱动的执行模型

## 技术栈

- **后端**：Golang（Go 1.24+）
  - 模块化设计
  - 偏 Clean Architecture / 分层架构
- **存储**：MySQL 8（InnoDB）
- **消息 / 队列**
  - Kafka
  - 任务队列（参考 asynq 的接口风格）
- **部署方式**
  - Kubernetes
  - Docker / Helm
  - 裸金属服务器

---

# Go 代码风格规范

## 基本要求

- 使用标准 `go fmt` / `goimports`
- 函数名、结构体名需清晰表达职责，避免过度缩写
- 对外暴露的公共方法、结构体、接口必须提供 GoDoc 注释
- 注释中**禁止使用阿拉伯数字序号**（如 `// 1. 2. 3.`）
- 错误处理必须显式返回 `error`，禁止滥用 `panic`
  - `panic` 仅允许在初始化阶段或不可恢复场景使用
- 日志统一使用项目内部封装的 `log` 包
- 任何涉及 IO / RPC / DB / 队列的接口必须显式传入 `context.Context`
- 使用 `sonic` 替代 `json.Marshal` / `json.Unmarshal`
- JSON 字段命名采用驼峰命名法，如 `json:"createdAt"`
- 与 ID 相关的字段或变量命名使用 `ID`
- 所有修改、新增的函数必须书写单元测试，禁止裸奔
- 所有函数必须添加注释
- http接口中尽量在service层打印日志，避免在controller层打印日志
- Repository 接口仅暴露领域语义方法，统一使用业务唯一标识（如 agentID）作为查询键，避免按数据库主键（id）重复定义方法，所有接口必须携带 context，并杜绝成对的 ByID / ByXXX 重复接口设计。
- Interface 定义必须以 I 开头，例如 IAgentRepository等。

## 并发约定

- 禁止直接使用 `go` 或 `go func`
- 所有 goroutine 必须通过 `safe.SafelyGo` 启动

---

# 结构设计原则

- 遵循 **单一职责原则**
  - 一个文件 / 结构体 / 函数只负责一个清晰的功能
- 公共逻辑应放在 `pkg` 或 `internal/pkg` 中
- 所有模型定义必须包含 `TableName` 方法，返回表名称
- `pkg` 包中不允许直接操作数据库，如果需要，在`pkg`包中定义 `interface`，在`internal/xxx/repo`包中实现
- 对外暴露能力时优先定义 `interface`
- 具体实现保持在内部，便于替换与测试

---

# 测试规范

- 新增核心模块时，尽量提供对应的 `*_test.go`
- 覆盖主要逻辑分支
- 使用 `go test` 原生测试框架
- 第三方测试库（如 testify）可使用但不得滥用

---

# 数据库与 SQL 约定

## 数据库配置

- MySQL 8
- 字符集与排序规则：`utf8mb4_0900_ai_ci`

## 表命名规范

- 统一使用 `t_xxx` 形式，例如：
  - `t_user`
  - `t_role`
  - `t_role_menu`
  - `t_menu`

## 通用字段约定

- `id`：自增主键
- `xxx_id`：业务唯一标识（UUID 字符串）
- `is_enabled`：`tinyint(1)`
- `is_deleted`：`tinyint(1)`
- `created_at`：`timestamp`
- `updated_at`：`timestamp`（带 `ON UPDATE CURRENT_TIMESTAMP`）

## SQL 编写规则

- 必须考虑索引与唯一约束
- 禁止使用 `SELECT *`
- 明确列出所需字段
- 批量初始化数据优先使用 `INSERT … SELECT`
- 数据库操作统一使用 **gorm**
  - 禁止直接手写 SQL
  - 查询时只选择需要的字段

---

# Agent / Executor / 任务队列

## Agent 职责

- 通过 gRPC 与平台端通信
- 从任务队列中拉取任务（Kafka / Redis 风格）
- 调用 Executor 执行任务
- 上报执行状态、日志与指标

## Executor 接口约定

```go
Execute(ctx context.Context, task *Task) (*Result, error)
```

要求：

- 支持超时与取消
- 支持日志与指标上报

## 任务队列设计

参考 asynq 的模型：

- 多队列优先级（Queues map[string]int）
- 严格优先级调度（StrictPriority）
- 支持延迟任务与定时任务
- 支持批量 / 分组任务执行

## 插件系统规范

- 基于接口的插件架构
- 双层注册机制
- 动作驱动的执行模型
- 内置 11 种预定义插件类型
- 统一的配置与生命周期管理

插件管理器负责：

- 插件注册
- 初始化
- 健康检查
- 生命周期管理

---

# Agent 行为规范

## 代码与设计类任务

当被要求：

- 补全代码
- 重构代码
- 设计接口
- 设计表结构
- 编写初始化 SQL

必须遵循：

- 充分理解已有上下文，尽量保持兼容
- 多种方案时优先选择 简单 + 可扩展
- 避免过度抽象
- 不随意打破已有命名与模块边界

## 文档类任务

当编写注释、文档或 README 时：

- 面向有经验的 Go / 后端 / DevOps 工程师
- 重点说明设计动机与关键约束
- 明确说明：
  - 幂等性
  - 事务边界
  - 并发模型

## SQL / 迁移类任务

- 确保 MySQL 8 语法兼容
- 涉及唯一约束或外键时，需明确说明冲突场景

## 默认语言约定

- 代码标识符使用英文
- 注释以英文为主，必要时可中英混合
- 输出需包含必要的设计解释，便于快速理解

## 角色定义

在本仓库中，Agent 同时承担以下角色：

- 首席工程师（Chief Engineer）
- 代码助手（Coding Assistant）
- 架构建议者（Architecture Advisor）
- 技术文档撰写者（Tech Writer）

所有行为必须以 **长期可维护性、清晰性与生产可用性** 为最高优先级。